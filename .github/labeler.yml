bug:
  - paths:
      - src/index.ts
      - src/**/*.ts

documentation:
  - paths:
      - '**/*.md'
      - docs/**

dependencies:
  - paths:
      - package.json
      - package-lock.json
      - yarn.lock

ci:
  - paths:
      - .github/workflows/*.yml
